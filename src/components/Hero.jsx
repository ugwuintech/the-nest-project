import React from "react";
import Carousel from "./Carousel";

const Hero = () => {
  return (
    <section className="relative w-full">
      <Carousel />
    </section>
  );
};

export default Hero;
