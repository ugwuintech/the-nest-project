import React from "react";
import Carousel from "./Carousel";

const Hero = () => {
  return (
    <section className="relative w-full h-screen">
      <Carousel />
    </section>
  );
};

export default Hero;
